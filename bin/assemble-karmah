#!/bin/env bash

libdir=$(dirname $(dirname $(readlink -f ${BASH_SOURCE[0]})))/lib
file="${1:-karmah}"


cat $libdir/pre.bash >"$file"
for lib in  $libdir/*/*.bash; do
    echo adding $lib
    printf '\n#####################################\n# %s\n' $(basename $lib) >>"$file"
    cat $lib  >>"$file"
done
printf '\n#####################################\nmain "${@}"\n' >>"$file"
chmod 755 "$file"
